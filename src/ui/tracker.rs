use tui::{
    backend::Backend,
    layout::{Layout, Constraint, Direction, Rect},
    widgets::{Widget, Block, Borders, Table, TableState},
    style::{Style, Color},
    Frame
};

/// draws the tracker widget? UI Element? 
pub fn draw_tracker<B: Backend> (f: Frame<B>) {
    /*
        The Tracker will be a sort of Table Widget.
    */
}